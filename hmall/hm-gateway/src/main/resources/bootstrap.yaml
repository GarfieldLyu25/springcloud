spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: 192.168.1.3
      config:
        file-extension: yaml
        shared-configs:
          - dataId: shared-log.yaml # 共享日志配置